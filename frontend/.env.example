# Copy this file to .env and fill in your Google OAuth client ID.
# Must be the same client ID as GOOGLE_CLIENT_ID in backend/.env — the
# frontend uses it to request a Google sign-in token, and the backend
# verifies that token was issued for this same client.
VITE_GOOGLE_CLIENT_ID=
